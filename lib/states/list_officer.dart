import 'package:flutter/material.dart';

class ListOfficer extends StatelessWidget {
  const ListOfficer({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('List Officer'),
      ),
    );
  }
}
